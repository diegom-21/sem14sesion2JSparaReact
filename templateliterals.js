console.log("-----------------------------------templateLiterals--------------------------------")
// Template Literals permiten incluir variables y expresiones dentro de strings.
// Utilizan el símbolo ` (backtick) en lugar de comillas simples o dobles.

const nombre = "Juan";
const edad = 30;

// Template literals
const saludo = `Hola, mi nombre es ${nombre} y tengo ${edad} años.`;

console.log(saludo);
